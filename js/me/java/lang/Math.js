js2me.createClass({
	$abs$I$I: Math.abs,
	$min$II$I: Math.min,
	$max$II$I: Math.max,
	$max$JJ$J: function (a, b) {
		if (a.toInt() > b.toInt()) {
			return a;
		} else {
			return b;
		}
	},
	package: 'javaRoot.$java.$lang',
	name: '$Math'
});
